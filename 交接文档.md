# Godot 编辑器插件「外部窗口」交接文档

## 项目概述

在 Godot 4.6.1 编辑器的 2D/3D/Script 选项卡同级位置添加「外部窗口」面板，支持将 Windows 系统中已打开的窗口通过 Win32 API 嵌入到编辑器中。切换到其他选项卡时隐藏嵌入窗口，不影响鼠标操作。

- **Godot 版本**: v4.6.1.stable.mono.official
- **目标平台**: Windows x86_64
- **发布目标**: Godot Asset Library
- **GitHub 仓库**: https://github.com/kakarot-china/godot_external_window_plugin

---

## 架构

采用 **GDExtension C++ + GDScript EditorPlugin 混合架构**：

- GDExtension C++ 无法直接注册 EditorPlugin（[godot#85268](https://github.com/godotengine/godot/issues/85268)），必须通过 GDScript 桥接
- C++ DLL 封装 Win32 API 操作（枚举窗口、窗口样式修改、位置管理）
- GDScript 负责 EditorPlugin 注册和 UI 交互

```
用户操作 → GDScript EditorPlugin → 调用 C++ WindowManager → Win32 API
```

### 嵌入方案（经过多次迭代）

最终方案：**不使用 SetParent**，而是将目标窗口改为无边框 WS_POPUP 弹出窗口，通过 `GWLP_HWNDPARENT` 设置 owner 为编辑器窗口，使弹出窗口自动停留在编辑器上方。`MoveWindow` 将其定位到面板区域的屏幕坐标。

**迭代历史（踩过的坑）：**

| 尝试 | 方案 | 问题 |
|------|------|------|
| 1 | `SetParent(target, godot窗口)` | 窗口被 Godot 的 Vulkan/OpenGL 渲染层完全遮挡，不可见 |
| 2 | 创建独立 WS_POPUP 容器 + `SetParent(target, 容器)` | 容器同样被 Godot 渲染层遮挡 |
| 3 | 无 SetParent，直接改样式为 WS_POPUP + `GWLP_HWNDPARENT` | 方向正确，但面板高度为 0 导致窗口不可见（见下方 bug） |

---

## 项目结构

```
F:/Develop/AI/claude/
├── .github/workflows/build.yml          # GitHub Actions CI（Windows 构建）
├── src/
│   ├── register_types.h                 # GDExtension 入口声明
│   ├── register_types.cpp               # GDExtension 注册（GDExtensionInterfaceGetProcAddress）
│   ├── window_manager.h                 # WindowManager 类声明
│   └── window_manager.cpp               # Win32 API 实现
├── addons/external_window/
│   ├── plugin.cfg                       # Godot 插件配置
│   ├── external_window.gdextension      # GDExtension 库描述
│   ├── scripts/
│   │   ├── external_window_plugin.gd    # EditorPlugin（主屏幕选项卡）
│   │   └── external_window_panel.gd     # 面板 UI（下拉框+容器）
│   ├── icons/external_window.svg        # 插件图标
│   └── bin/
│       ├── *.dll (debug)                # GitHub Actions 构建产物
│       └── *.dll (release)
├── godot-cpp/                           # git submodule（最新 HEAD）
├── project.godot                        # 测试项目配置
├── SConstruct                           # SCons 构建配置
└── .gitignore
```

---

## 已完成的工作

### 初始提交（6 次 commit）

| Commit | 内容 |
|--------|------|
| `56f136a` | 初始提交：完整项目结构和所有源文件 |
| `2f89f45` | 修复 SConstruct：使用 env["suffix"] 替代手动拼接 |
| `951804d` | 修复 SConstruct：显式包含 env["SHLIBSUFFIX"] 解决 DLL 后缀冲突 |
| `fb0c8bb` | 修复 GDExtension 入口函数：`GDExtensionInterface*` → `GDExtensionInterfaceGetProcAddress` |
| `b4fb8cf` | 修复 GDScript：`get_content_scale_factor()` → `EditorInterface.get_editor_scale()` |

### 功能修复（7 次 commit）

| Commit | 内容 |
|--------|------|
| `1c16d3b` | 修复 HWND 常量（硬编码 1→`DisplayServer.WINDOW_HANDLE`）、PID 过滤、resize 跟踪、焦点管理 |
| `76ab9ab` | 修复坐标计算（去掉 window_pos 偏移）、保存/恢复原始窗口位置、刷新保留已嵌入选项 |
| `f232306` | 改用弹出式容器窗口（SetParent 方案被渲染层遮挡） |
| `0d79206` | 改用 GWLP_HWNDPARENT 无边框弹出窗口方案（不使用 SetParent） |
| `904f115` | 用 `ClientToScreen` 获取客户区精确坐标替代 `window_get_position()` |
| `60cd8fe` | 添加 [EW] 调试日志 |
| `a07fdaa` | **关键修复**：根 Control 缺少 `PRESET_FULL_RECT` 导致面板高度为 0 |

---

## 踩过的坑

### 1. SConstruct DLL 命名错误
```
scons: *** A shared library should have exactly one target with the suffix: .dll
```
**原因**: 手动拼接带多个点号的目标名，SCons 后缀处理冲突
**解决**: 使用官方模板模式 `env["suffix"] + env["SHLIBSUFFIX"]`

### 2. GDExtension 入口函数签名变更
```
error C2065: 'GDExtensionInterface': undeclared identifier
```
**原因**: godot-cpp 4.2+ 将第一个参数从 `GDExtensionInterface*` 改为 `GDExtensionInterfaceGetProcAddress`
**解决**: 更新 `register_types.cpp` 中的函数签名

### 3. GDScript API 不存在
```
Function "get_content_scale_factor()" not found in base self
```
**原因**: `get_content_scale_factor()` 是 Window 的方法，不是 Control 的
**解决**: 使用 `EditorInterface.get_editor_scale()`

### 4. SetParent 嵌入被 Godot 渲染层遮挡
**原因**: Godot 使用 Vulkan/OpenGL 渲染覆盖整个客户区，Win32 子窗口在渲染层之下不可见
**解决**: 放弃 SetParent 方案，改用 WS_POPUP + GWLP_HWNDPARENT

### 5. 面板高度为 0 导致嵌入窗口不可见
**现象**: 日志显示 `panel_rect S: (2546.0, 0.0)`，嵌入窗口被创建为 `5092x0`
**原因**: 根 Control 没有设置 `PRESET_FULL_RECT`，没有铺满编辑器主屏幕，VBoxContainer 和 Panel 都没有高度
**解决**: 在 `_ready()` 开头添加 `set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)`

### 6. 坐标偏移：window_get_position 包含标题栏
**现象**: 嵌入窗口位置偏上约 45 像素
**原因**: `DisplayServer.window_get_position()` 返回窗口框架坐标（含标题栏），而 Godot 视口从客户区开始
**解决**: 新增 `get_client_position()` 方法，用 Win32 `ClientToScreen` 获取客户区精确屏幕坐标

### 7. 面板高度为 0（第二次）：PRESET_FULL_RECT 对 VBoxContainer 无效
**现象**: `a07fdaa` 添加 `PRESET_FULL_RECT` 后，日志仍显示 `panel_rect S: (2546.0, 0.0)`
**原因**: `EditorInterface.get_editor_main_screen()` 返回的是 VBoxContainer，VBoxContainer 通过 `size_flags` 分配子节点空间，anchor/preset 对其无效
**解决**: 在根 Control 上添加 `size_flags_horizontal = Control.SIZE_EXPAND_FILL` 和 `size_flags_vertical = Control.SIZE_EXPAND_FILL`

### 8. VS Code 取消嵌入后窗口异常
**现象**: 取消嵌入后 VS Code 窗口位置和大小不正确（停留在嵌入时的 5092x0）
**原因**: 嵌入时 VS Code 处于最小化状态（`showCmd=2`），`SetWindowPlacement` 恢复为最小化，后续 `ShowWindow(SW_SHOW)` 无法正确恢复位置
**解决**: 取消嵌入时强制 `showCmd = SW_SHOWNORMAL`，使窗口以正常状态恢复到原始位置

---

## 当前状态

- [x] 项目结构搭建完成
- [x] C++ GDExtension 核心代码编写完成
- [x] GDScript EditorPlugin 编写完成
- [x] GitHub Actions CI 构建通过
- [x] DLL 已构建并下载
- [x] HWND 常量修复
- [x] PID 过滤替代 HWND 过滤
- [x] resize 跟踪
- [x] 刷新下拉保留已嵌入选项
- [x] 坐标计算修复（ClientToScreen）
- [x] 面板零高度修复（PRESET_FULL_RECT）— 第一次尝试，未生效
- [x] 面板零高度修复（SIZE_EXPAND_FILL）— 根因修复
- [x] VS Code 取消嵌入后窗口异常修复（强制 SW_SHOWNORMAL）
- [ ] **功能验证进行中**：待下载最新 CI 构建的 DLL 测试

### 5/20 测试结果与修复

`a07fdaa` 的 `PRESET_FULL_RECT` 修复**未生效**，测试日志仍显示 `panel_rect S: (2546.0, 0.0)` 高度为 0。

**根因分析**：`EditorInterface.get_editor_main_screen()` 返回的是 **VBoxContainer**，VBoxContainer 不通过 anchor 系统分配子节点高度，而是通过 `size_flags` 决定。`PRESET_FULL_RECT` 只设置了 anchors，对 VBoxContainer 的子节点无效。

**修复方案**：在根 Control 上添加 `size_flags_horizontal = Control.SIZE_EXPAND_FILL` 和 `size_flags_vertical = Control.SIZE_EXPAND_FILL`。

同时修复了 VS Code 取消嵌入后窗口异常的问题：原代码恢复窗口原始 `showCmd`（最小化），导致窗口位置/大小不正确。改为强制 `SW_SHOWNORMAL`，确保取消嵌入后窗口以正常状态显示在原始位置。

**待验证**：
1. 下载 Actions 构建的 DLL
2. 在 Godot 4.6.1 中测试
3. 检查 `[EW]` 日志确认 `panel_rect` 高度非零
4. 验证文件管理器等常规窗口能正确嵌入并定位
5. 验证 VS Code 取消嵌入后窗口恢复正常

---

## 待完成 / 已知问题

### 功能层面

1. **VS Code（Electron/Chromium）可能无法嵌入**：Chromium 使用 GPU 加速渲染，改窗口样式后可能停止渲染。日志显示嵌入操作本身成功（style/owner/visible 都正确），但窗口内容不显示。这是 Chromium 的已知限制，需要调研其他嵌入方案（如 CEF 嵌入、Chromium `--force-renderer-accessibility` 等）
2. **取消嵌入后 VS Code 窗口异常**：`SetWindowPlacement` 恢复了样式和位置，但 VS Code 的自定义标题栏（Electron）可能没有正确响应样式恢复
3. **调试日志待清理**：`60cd8fe` 添加的 `[EW]` 日志用于诊断，功能验证完成后需移除
4. **编辑器窗口移动**：定时器每 0.5s 同步位置，可能有轻微延迟。可优化为更短的间隔或监听窗口移动信号
5. **DPI 缩放**：坐标转换使用了 `EditorInterface.get_editor_scale()`，高 DPI 下需验证

### 发布准备

1. **plugin.cfg 中的 author 需要修改**：当前是占位符 "Author"
2. **多平台支持**：当前仅 Windows
3. **godot-cpp 版本锁定**：当前使用最新 HEAD，正式发布时应锁定到具体 commit/tag
4. **README.md**：发布到 Asset Library 需要说明文档
5. **LICENSE 文件**：需要添加开源协议
6. **DLL 分发方式**：当前通过 GitHub Actions artifact 下载，需确定最终策略

---

## 关键文件说明

| 文件 | 重要性 | 说明 |
|------|--------|------|
| `src/window_manager.cpp` | 核心 | Win32 窗口嵌入的全部逻辑，修改嵌入行为从这里改 |
| `src/register_types.cpp` | 入口 | GDExtension 初始化，暴露 WindowManager 给 GDScript |
| `addons/external_window/scripts/external_window_panel.gd` | 核心 | 面板 UI 和交互逻辑，坐标计算、下拉框管理、定时器同步 |
| `addons/external_window/scripts/external_window_plugin.gd` | 入口 | EditorPlugin 注册，主屏幕选项卡生命周期管理 |
| `SConstruct` | 构建 | SCons 构建配置 |
| `.github/workflows/build.yml` | CI | GitHub Actions 构建流程 |

---

## 快速恢复指南

### 本地开发环境搭建
```bash
# 1. 克隆仓库
git clone https://github.com/kakarot-china/godot_external_window_plugin.git
cd godot_external_window_plugin
git submodule update --init --recursive

# 2. 安装 SCons
pip install scons

# 3. 安装 Visual Studio Build Tools（MSVC C++ 编译器）

# 4. 构建
scons target=template_debug    # debug 版本
scons target=template_release  # release 版本
```

### 在 Godot 中测试
1. 用 Godot 4.6.1 打开项目根目录
2. 项目 → 项目设置 → 插件 → 启用 "External Window"
3. 编辑器顶部出现「外部窗口」选项卡

### 修改 C++ 代码后
1. 重新运行 `scons` 构建 DLL
2. 重启 Godot 编辑器（或禁用再启用插件）

### 仅修改 GDScript
1. 在编辑器中禁用再启用插件即可热加载
